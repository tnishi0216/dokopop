<Window x:Class="amsocr.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:amsocr"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!-- 右クリック用コンテキストメニューを追加 -->
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem x:Name="miCapturePage" Header="Capture Page" IsCheckable="True" />
            <MenuItem x:Name="miUseDefLang" Header="Use Default Language" IsCheckable="True" />
            <Separator />
            <MenuItem x:Name="miDebugMode" Header="Debug Mode" IsCheckable="True" />
        </ContextMenu>
    </Window.ContextMenu>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="66" />
            <RowDefinition Height="39" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <!--  ヘッダー  -->
        <Label Name="lbPoint" Content="(Point)" HorizontalAlignment="Left" VerticalAlignment="Top" Width="60" Height="25" Margin="102,0,0,0"/>
        <Label Name="lbStatus" Content="(Status)" HorizontalAlignment="Left" VerticalAlignment="Top" Width="611" Height="25" Margin="167,0,0,0"/>
        <TextBox x:Name="tbInfo"
                 Text="(Information)"
           DockPanel.Dock="Bottom"
           IsReadOnly="True"
           AcceptsReturn="True"
           TextWrapping="Wrap"
           VerticalScrollBarVisibility="Auto"
           Margin="102,25,6,0"
           FontFamily="Consolas"
           FontSize="12"/>
        <Label
            x:Name="lbPath"
            Grid.Row="1"
            Width="92"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            HorizontalContentAlignment="Right"
            Content="ファイルパス：" Height="26" />
        <TextBox
            x:Name="txtPath"
            Grid.Row="1"
            Margin="102,0,183,0"
            VerticalAlignment="Center" Height="18"
            />
        <Button
            Name="btnPath"
            Grid.Row="1"
            Width="70"
            Margin="630,0,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Click="btnPath_Click"
            Content="参照..."
            FontSize="12" Height="20"
            />
        <Button
            Name="btnOcr"
            Grid.Row="1"
            Width="85"
            Margin="705,0,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Click="btnOcr_Click"
            Content="OCR実行"
            FontSize="12" Height="20" />
        <Image
            x:Name="ImgTarget"
            Grid.Row="2"
            Margin="10,10,10,100" />
        <TextBox
            Name="txtOcrResult"
            Grid.Row="2"
            Margin="10,238,10,0"
            VerticalAlignment="Top"
            
            AcceptsReturn="True"
            IsEnabled="{Binding Path=IsChecked, ElementName=MaterialDesignOutlinedTextBoxEnabledComboBox}"
            
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto" Height="81" />
    </Grid>
</Window>
